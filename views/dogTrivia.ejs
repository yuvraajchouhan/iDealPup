<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-KK94CHFLLe+nY2dmCWGMq91rCGa5gtU4mk92HdvYe+M/SXH301p5ILy+dN9+nJOZ" crossorigin="anonymous">
    <title>Dog Trivia</title>
</head>

<body>
    <%- include("templates/header") %>

        <main>
            <div class="text-center py-5">
                <h1 class="display-3 fw-bold mb-5" style="font-size:40px">Dog Trivia</h1>
                <div style="font-size:24px">
                    <% if (round> 1) { %>
                        <% if (!answerStatus) { %>
                            <div class="bookmarkFeedback" style="background-color: rgb(240, 190, 98); 
                text-align: center; margin: 10px; border-radius: 10px ">
                                <%= answerFeedback %>
                            </div>
                            <% } else { %>
                                <div class="bookmarkFeedback" style="background-color: rgb(94, 202, 103); 
                text-align: center; margin: 10px; border-radius: 10px">
                                    <%= answerFeedback %>
                                </div>
                                <% } %>
                                    <% } %>
                </div>

                <div class="container">
                    <div class="gameInfo">
                        <p class="fw-bold text-success" style="font-size:40px"> Round <%= round %>
                        </p>
                    </div>

                    <div style="display: flex; justify-content: center; height: 30vh">
                        <img src="/pups/<%= correctAnswer %>.png" style="align-self: center;
              height:100%">
                    </div>
                    

                    <div class="mt-5">
                        <p class="mb-5" style="font-size:30px"> What is the name of this dog breed? </p>
                        <% let i=1; %>
                            <% for(i=1; i < result.length + 1; i++) { %>
                                <% if (result[i-1]==correctAnswer) { %>
                                    <button class="btn btn-success mb-5" style="font-size:24px"
                                        onclick="window.location.href='/dogTriviaConfigureAnswer?answer=<%= correctAnswer %>'">
                                        <%= i %>. <%= result[i-1] %>
                                    </button><br>
                                    <% } else { %>
                                        <button class="btn btn-success mb-5" style="font-size:24px"
                                            onclick="window.location.href='/dogTriviaConfigureAnswer?answer=<%= result[i-1] %>'">
                                            <%= i %>. <%= result[i-1] %>
                                        </button><br>
                                        <% } %>
                                            <% } %>
                    </div>
                </div>
            </div>

        </main>

        <%- include("templates/footer") %>