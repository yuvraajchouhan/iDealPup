<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-KK94CHFLLe+nY2dmCWGMq91rCGa5gtU4mk92HdvYe+M/SXH301p5ILy+dN9+nJOZ"
      crossorigin="anonymous">
    <title>Bookmark</title>
  </head>

  <body>

    <%- include("templates/header") %>

    <main style="font-size: 40px;">

      <section class="py-5 text-center">
        <div class="row py-lg-5">
          <div class="col-lg-6 col-md-10 mx-auto">
            <h1 class="display-3 fw-bold mb-5"> Bookmarks </h1>
            <p class="text-body-secondary">
              <%= name %>'s bookmarks. <br>
              Click on the card to view the dog breed! <br>
              Click on 'Unbookmark' to remove the dog breed from your bookmarks.
            </p>
          </div>
        </div>
      </section>

      <% if (bookmarkFeedback !== "") { %>
      <div class="bookmarkFeedback" style="background-color: rgb(94, 202, 103); 
      text-align: center; margin: 10px;">
        <%= bookmarkFeedback %>
      </div>
      <% } %>

      <div class="album py-5">
        <div class="mx-5">
          <div class="row row-cols-1 row-cols-md-2 g-4">

            <% let i = 1; %>
            <% while (user[`bookmark${i}`]) { %>
            <% if (user[`bookmark${i}`] !== ".") { %>
            <%- include('templates/bookmarkCard', { bookmarkedDog:
            user[`bookmark${i}`] }); %>
            <% } %>
            <% i++; %>
            <% } %>

          </div>
        </div>
      </div>

    </main>

    <%- include("templates/footer") %>